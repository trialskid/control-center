services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - blaine-data:/app/data
      - blaine-media:/app/media
    restart: unless-stopped

volumes:
  blaine-data:
  blaine-media:
